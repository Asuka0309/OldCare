# 社区活动与资讯模块 - 详细分析

## 📌 模块概述

**模块名称**：社区活动与资讯  
**核心功能**：发布和管理社区活动、老人报名参加、资讯资讯聚合  
**适用角色**：管理员（发布/管理活动）、居民（查看/报名活动）  
**关键特性**：活动发布、报名管理、参与人数统计、资讯聚合、状态管理  
**优先级**：⭐⭐ 社区互动和信息传播

---

## 🎯 核心概念

### 1. 社区活动定义

社区活动是**养老社区组织的各类活动**，包括讲座、健身、娱乐等，老人可以报名参加。

```
社区活动 = 活动信息 + 报名管理 + 参与人数 + 状态管理
```

**实体字段**：
```java
CommunityActivity {
  id                        // 活动ID
  organizerId               // 组织者ID（管理员）
  
  // 活动信息
  activityTitle             // 活动标题
  activityDescription       // 活动详细描述
  activityType              // 活动类型（讲座/健身/娱乐/交流/其他）
  activityDate              // 活动日期时间
  location                  // 活动地点
  
  // 参与管理
  maxParticipants           // 最大参与人数
  currentParticipants       // 当前参与人数（实时计算）
  
  // 状态管理
  status                    // 活动状态（draft/published/completed/cancelled）
  
  // 时间戳
  createdAt                 // 创建时间
  updatedAt                 // 更新时间
}

ActivityRegistration {
  id                        // 报名记录ID
  activityId                // 活动ID
  residentId                // 老人ID
  registrationStatus        // 报名状态（registered/attended/cancelled）
  createdTime               // 报名时间
  updatedTime               // 更新时间
}
```

### 2. 活动类型

| 类型 | 说明 | 举例 |
|------|------|------|
| **lecture** | 讲座 | 养生讲座、法律知识讲座 |
| **fitness** | 健身 | 太极班、散步团 |
| **entertainment** | 娱乐 | 卡拉OK、电影放映 |
| **exchange** | 交流 | 居民座谈会、经验分享 |
| **other** | 其他 | 其他类型活动 |

### 3. 活动状态机

```
【draft】草稿
  └─ 管理员未发布的活动
  
【published】进行中
  ├─ 活动正式发布，老人可以报名
  ├─ 可以编辑、完成、取消
  └─ 常规运行状态

【completed】已完成
  └─ 活动已举行完毕
  └─ 不可再编辑或取消

【cancelled】已取消
  └─ 活动被取消（可能有突发情况）
  └─ 不可再编辑
```

### 4. 报名状态

```
【registered】已报名
  └─ 老人已报名参加活动

【attended】已参加
  └─ 老人实际参加了活动（后续标记）

【cancelled】已取消
  └─ 老人取消了报名
```

---

## 📡 API 接口

### 1. 查询接口

#### 1.1 获取活动列表
```
GET /api/activities?page=1&size=10&status=published&type=lecture&keyword=养生

参数：
  - page: 当前页码（默认1）
  - size: 每页条数（默认10）
  - status: 活动状态筛选（published/completed/cancelled，可选）
  - type: 活动类型筛选（lecture/fitness/entertainment/exchange/other，可选）
  - keyword: 标题关键词搜索（可选）

响应：{
  "code": 0,
  "message": "成功",
  "data": {
    "records": [
      {
        "id": 1,
        "organizerId": 1,
        "activityTitle": "养生讲座",
        "activityDescription": "专家讲解老年养生知识...",
        "activityType": "lecture",
        "activityDate": "2025-12-28 14:00:00",
        "location": "会议室101",
        "maxParticipants": 50,
        "currentParticipants": 32,  // 当前报名人数
        "status": "published",
        "createdAt": "2025-12-20 10:00:00",
        "updatedAt": "2025-12-20 10:00:00"
      }
    ],
    "total": 15,
    "current": 1,
    "size": 10
  }
}
```

**权限规则**：
```java
// 非管理员默认只看"published"状态
if (!"ADMIN".equalsIgnoreCase(userRole)) {
  if (status == null) {
    wrapper.eq("status", "published");
  }
}

// 管理员可以看所有状态
```

#### 1.2 获取活动详情
```
GET /api/activities/{id}

响应：{
  "code": 0,
  "message": "成功",
  "data": {
    "id": 1,
    "activityTitle": "养生讲座",
    "currentParticipants": 32,  // 实时计算
    ...
  }
}
```

### 2. 管理接口（管理员）

#### 2.1 发布活动
```
POST /api/activities
请求体：{
  "activityTitle": "养生讲座",
  "activityType": "lecture",
  "activityDate": "2025-12-28 14:00:00",
  "location": "会议室101",
  "maxParticipants": 50,
  "activityDescription": "专家讲解老年养生知识..."
}

响应：{
  "code": 0,
  "message": "发布成功",
  "data": {
    "id": 1,
    "status": "published",
    "createdAt": "2025-12-20 10:00:00"
  }
}
```

**后端逻辑**：
```java
@PostMapping
public ApiResponse<CommunityActivity> create(@RequestBody CommunityActivity activity) {
  Long userId = UserContext.getUserId();
  
  // 1. 自动设置组织者
  activity.setOrganizerId(userId);
  
  // 2. 自动设置初始状态
  activity.setStatus("published");  // 直接发布
  
  // 3. 记录时间戳
  activity.setCreatedAt(LocalDateTime.now());
  activity.setUpdatedAt(LocalDateTime.now());
  
  // 4. 保存到数据库
  boolean success = activityService.save(activity);
  if (success) {
    return ApiResponse.success("发布成功", activity);
  }
  return ApiResponse.error("发布失败");
}
```

#### 2.2 编辑活动
```
PUT /api/activities/{id}
请求体：{
  "activityTitle": "养生讲座（更新）",
  "activityDescription": "更新后的描述",
  "maxParticipants": 60,
  ...
}

响应：{
  "code": 0,
  "message": "更新成功"
}
```

**限制**：
- ✅ 仅"published"状态可以编辑
- ✅ "completed"和"cancelled"不可编辑

#### 2.3 取消活动
```
PUT /api/activities/{id}/cancel

响应：{
  "code": 0,
  "message": "已取消"
}
```

**规则**：
```java
// 不能取消已完成的活动
if ("completed".equals(activity.getStatus())) {
  return ApiResponse.error("已完成的活动不能取消");
}

// 只有已发布的可以取消
if (!"published".equals(activity.getStatus())) {
  return ApiResponse.error("该活动不能取消");
}
```

#### 2.4 标记为完成
```
PUT /api/activities/{id}/complete

响应：{
  "code": 0,
  "message": "已标记为完成"
}
```

#### 2.5 删除活动
```
DELETE /api/activities/{id}

响应：{
  "code": 0,
  "message": "删除成功"
}
```

### 3. 报名接口（居民）

#### 3.1 报名参加活动
```
POST /api/activities/{id}/register

响应：{
  "code": 0,
  "message": "报名成功"
}
```

**后端逻辑**：
```java
@PostMapping("/{id}/register")
public ApiResponse<String> register(@PathVariable Long id) {
  // 1. 验证活动存在且状态可用
  CommunityActivity activity = activityService.getById(id);
  if (activity == null) {
    return ApiResponse.error("活动不存在");
  }
  
  // 2. 验证活动状态可报名
  if (!"published".equals(activity.getStatus())) {
    return ApiResponse.error("该活动不可报名");
  }
  
  Long userId = UserContext.getUserId();
  
  // 3. 验证未重复报名
  if (registrationService.isRegistered(id, userId)) {
    return ApiResponse.error("您已经报名过此活动");
  }
  
  // 4. 验证报名人数未满
  int currentCount = registrationService.countByActivityId(id);
  if (activity.getMaxParticipants() != null && currentCount >= activity.getMaxParticipants()) {
    return ApiResponse.error("报名人数已满");
  }
  
  // 5. 创建报名记录
  ActivityRegistration registration = new ActivityRegistration();
  registration.setActivityId(id);
  registration.setResidentId(userId);
  registration.setRegistrationStatus("registered");
  registration.setCreatedTime(LocalDateTime.now());
  registration.setUpdatedTime(LocalDateTime.now());
  
  boolean success = registrationService.save(registration);
  if (success) {
    return ApiResponse.success("报名成功");
  }
  return ApiResponse.error("报名失败");
}
```

**关键验证**：
- ✅ 活动必须是"published"状态
- ✅ 不能重复报名
- ✅ 报名人数不能超过最大人数

#### 3.2 取消报名
```
DELETE /api/activities/{id}/register

响应：{
  "code": 0,
  "message": "已取消报名"
}
```

**后端逻辑**：
```java
@DeleteMapping("/{id}/register")
public ApiResponse<String> unregister(@PathVariable Long id) {
  // 1. 验证活动存在
  CommunityActivity activity = activityService.getById(id);
  if (activity == null) {
    return ApiResponse.error("活动不存在");
  }
  
  Long userId = UserContext.getUserId();
  
  // 2. 查找报名记录
  QueryWrapper<ActivityRegistration> wrapper = new QueryWrapper<>();
  wrapper.eq("activity_id", id);
  wrapper.eq("resident_id", userId);
  wrapper.eq("registration_status", "registered");
  
  ActivityRegistration registration = registrationService.getOne(wrapper);
  if (registration == null) {
    return ApiResponse.error("您未报名此活动");
  }
  
  // 3. 更新状态为已取消
  registration.setRegistrationStatus("cancelled");
  registration.setUpdatedTime(LocalDateTime.now());
  
  boolean success = registrationService.updateById(registration);
  if (success) {
    return ApiResponse.success("已取消报名");
  }
  return ApiResponse.error("取消失败");
}
```

---

## 🎨 前端页面（CommunityActivityView.vue）

### 1. 页面布局

#### 1.1 顶部操作栏
```vue
<div class="card-header">
  <span class="title">社区活动</span>
  <!-- 仅管理员显示"新建活动"按钮 -->
  <el-button v-if="isAdmin" type="primary" @click="handleAdd">
    <i class="el-icon-plus"></i>
    新建活动
  </el-button>
</div>
```

#### 1.2 筛选栏

```vue
<el-row :gutter="20" class="search-row">
  <!-- 1. 按状态筛选 -->
  <el-col :span="6">
    <el-select v-model="filters.status" placeholder="请选择活动状态" clearable>
      <el-option label="全部" value=""></el-option>
      <el-option label="进行中" value="published"></el-option>
      <el-option label="已完成" value="completed"></el-option>
      <el-option label="已取消" value="cancelled"></el-option>
    </el-select>
  </el-col>
  
  <!-- 2. 按类型筛选 -->
  <el-col :span="6">
    <el-select v-model="filters.type" placeholder="请选择活动类型" clearable>
      <el-option label="讲座" value="lecture"></el-option>
      <el-option label="健身" value="fitness"></el-option>
      <el-option label="娱乐" value="entertainment"></el-option>
      <el-option label="交流" value="exchange"></el-option>
      <el-option label="其他" value="other"></el-option>
    </el-select>
  </el-col>
  
  <!-- 3. 按关键词搜索 -->
  <el-col :span="6">
    <el-input v-model="filters.keyword" placeholder="输入标题关键词" clearable />
  </el-col>
  
  <!-- 4. 操作按钮 -->
  <el-col :span="6" class="text-right">
    <el-button @click="handleReset">重置</el-button>
  </el-col>
</el-row>
```

#### 1.3 活动列表表格

| 列 | 宽度 | 说明 |
|----|------|------|
| 活动标题 | 150px+ | 活动名称 |
| 活动类型 | 120px | 讲座/健身/娱乐/交流/其他 |
| 活动日期 | 120px | 活动日期 |
| 最大人数 | 100px | 容纳最多人数 |
| 已报名 | 100px | 当前报名人数（实时） |
| 状态 | 100px | 进行中/已完成/已取消 |
| 操作 | 250px | 查看/编辑/报名/取消报名/完成/取消/删除 |

**状态颜色映射**：
```javascript
const getStatusColor = (status) => {
  const map = {
    published: 'success',    // 绿色 - 进行中
    completed: 'info',       // 蓝色 - 已完成
    cancelled: 'danger'      // 红色 - 已取消
  }
  return map[status] || ''
}
```

#### 1.4 操作按钮规则

```vue
<!-- 查看：所有人都可以查看 -->
<el-button link type="primary" @click="handleView(row)">查看</el-button>

<!-- 编辑：仅管理员且活动进行中 -->
<el-button v-if="isAdmin && row.status === 'published'" link type="primary">编辑</el-button>

<!-- 报名：非管理员且活动进行中且未报名 -->
<el-button v-if="!isAdmin && row.status === 'published' && !isRegistered(row.id)" 
           link type="success" @click="handleRegister(row)">报名</el-button>

<!-- 取消报名：非管理员且已报名 -->
<el-button v-if="!isAdmin && isRegistered(row.id)" 
           link type="danger" @click="handleUnregister(row)">取消报名</el-button>

<!-- 完成：仅管理员且活动进行中 -->
<el-button v-if="isAdmin && row.status === 'published'" 
           link type="warning" @click="handleComplete(row)">完成</el-button>

<!-- 取消：仅管理员且活动进行中 -->
<el-button v-if="isAdmin && row.status === 'published'" 
           link type="danger" @click="handleCancel(row)">取消</el-button>

<!-- 删除：仅管理员 -->
<el-button v-if="isAdmin" link type="danger" @click="handleDelete(row)">删除</el-button>
```

**权限矩阵**：
| 操作 | admin | resident |
|------|-------|----------|
| 查看 | ✅ | ✅ |
| 新建 | ✅ | ❌ |
| 编辑 | ✅ | ❌ |
| 报名 | ❌ | ✅ |
| 取消报名 | ❌ | ✅ |
| 完成 | ✅ | ❌ |
| 取消 | ✅ | ❌ |
| 删除 | ✅ | ❌ |

### 2. 新建/编辑活动对话框

```vue
<el-dialog
  v-model="dialogVisible"
  :title="isEdit ? '编辑活动' : '新建活动'"
  width="600px"
>
  <el-form :model="formData" :rules="rules" label-width="120px">
    <!-- 活动标题（必填） -->
    <el-form-item label="活动标题" prop="activityTitle">
      <el-input v-model="formData.activityTitle" placeholder="如：养生讲座"></el-input>
    </el-form-item>
    
    <!-- 活动类型（必填） -->
    <el-form-item label="活动类型" prop="activityType">
      <el-select v-model="formData.activityType" placeholder="请选择活动类型">
        <el-option label="讲座" value="lecture"></el-option>
        <el-option label="健身" value="fitness"></el-option>
        <el-option label="娱乐" value="entertainment"></el-option>
        <el-option label="交流" value="exchange"></el-option>
        <el-option label="其他" value="other"></el-option>
      </el-select>
    </el-form-item>
    
    <!-- 活动日期（必填） -->
    <el-form-item label="活动日期" prop="activityDate">
      <el-date-picker v-model="formData.activityDate" type="date"></el-date-picker>
    </el-form-item>
    
    <!-- 活动位置（必填） -->
    <el-form-item label="活动位置" prop="location">
      <el-input v-model="formData.location" placeholder="如：会议室101"></el-input>
    </el-form-item>
    
    <!-- 最大参与人数（必填） -->
    <el-form-item label="最大参与人数" prop="maxParticipants">
      <el-input-number v-model="formData.maxParticipants" :min="1" :max="999"></el-input-number>
    </el-form-item>
    
    <!-- 活动描述（必填） -->
    <el-form-item label="活动描述" prop="activityDescription">
      <el-input
        v-model="formData.activityDescription"
        type="textarea"
        :rows="4"
        placeholder="请详细描述活动内容"
      ></el-input>
    </el-form-item>
  </el-form>
  
  <template #footer>
    <el-button @click="dialogVisible = false">取消</el-button>
    <el-button type="primary" @click="handleSave">保存</el-button>
  </template>
</el-dialog>
```

**表单验证规则**：
```javascript
const rules = {
  activityTitle: [
    { required: true, message: '请输入活动标题', trigger: 'blur' }
  ],
  activityType: [
    { required: true, message: '请选择活动类型', trigger: 'change' }
  ],
  activityDate: [
    { required: true, message: '请选择活动日期', trigger: 'change' }
  ],
  location: [
    { required: true, message: '请输入活动位置', trigger: 'blur' }
  ],
  maxParticipants: [
    { required: true, message: '请输入最大人数', trigger: 'blur' }
  ],
  activityDescription: [
    { required: true, message: '请输入活动描述', trigger: 'blur' }
  ]
}
```

### 3. 查看详情对话框

```vue
<el-dialog v-model="viewDialogVisible" title="活动详情" width="600px">
  <el-descriptions :column="1" border>
    <el-descriptions-item label="活动标题">
      {{ viewData.activityTitle }}
    </el-descriptions-item>
    
    <el-descriptions-item label="活动类型">
      <el-tag>{{ getTypeLabel(viewData.activityType) }}</el-tag>
    </el-descriptions-item>
    
    <el-descriptions-item label="活动日期">
      {{ formatDate({}, viewData.activityDate) }}
    </el-descriptions-item>
    
    <el-descriptions-item label="活动位置">
      {{ viewData.location }}
    </el-descriptions-item>
    
    <el-descriptions-item label="最大人数">
      {{ viewData.maxParticipants }}
    </el-descriptions-item>
    
    <el-descriptions-item label="已报名">
      {{ viewData.currentParticipants }}
    </el-descriptions-item>
    
    <el-descriptions-item label="活动描述">
      {{ viewData.activityDescription }}
    </el-descriptions-item>
    
    <el-descriptions-item label="状态">
      <el-tag :type="getStatusColor(viewData.status)">
        {{ getStatusLabel(viewData.status) }}
      </el-tag>
    </el-descriptions-item>
    
    <el-descriptions-item label="创建时间">
      {{ formatTime({}, viewData.createdAt) }}
    </el-descriptions-item>
  </el-descriptions>
</el-dialog>
```

---

## 🗄️ 数据库设计

### 1. community_activities 表

```sql
CREATE TABLE community_activities (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  admin_id BIGINT,
  activity_title VARCHAR(255) NOT NULL,
  activity_description LONGTEXT,
  activity_type VARCHAR(50),
  activity_date DATETIME,
  location VARCHAR(255),
  max_participants INT,
  status VARCHAR(50) DEFAULT 'published',
  created_time TIMESTAMP NOT NULL,
  updated_time TIMESTAMP NOT NULL,
  INDEX idx_admin_id (admin_id),
  INDEX idx_activity_type (activity_type),
  INDEX idx_activity_date (activity_date),
  INDEX idx_status (status),
  FOREIGN KEY (admin_id) REFERENCES users(id)
);
```

### 2. activity_registrations 表

```sql
CREATE TABLE activity_registrations (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  activity_id BIGINT NOT NULL,
  resident_id BIGINT NOT NULL,
  registration_status VARCHAR(50),
  created_time TIMESTAMP NOT NULL,
  updated_time TIMESTAMP NOT NULL,
  INDEX idx_activity_id (activity_id),
  INDEX idx_resident_id (resident_id),
  INDEX idx_registration_status (registration_status),
  UNIQUE KEY uk_activity_resident (activity_id, resident_id),
  FOREIGN KEY (activity_id) REFERENCES community_activities(id),
  FOREIGN KEY (resident_id) REFERENCES users(id)
);
```

**关键索引**：
- `uk_activity_resident`：确保同一老人不会重复报名同一活动
- `idx_activity_id`、`idx_resident_id`：快速查询报名情况

### 3. 字段详解

**community_activities**：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 活动ID |
| admin_id | BIGINT | 组织者ID |
| activity_title | VARCHAR(255) | 活动标题 |
| activity_description | LONGTEXT | 活动描述 |
| activity_type | VARCHAR(50) | 活动类型 |
| activity_date | DATETIME | 活动日期时间 |
| location | VARCHAR(255) | 活动地点 |
| max_participants | INT | 最大参与人数 |
| status | VARCHAR(50) | 活动状态 |
| created_time | TIMESTAMP | 创建时间 |
| updated_time | TIMESTAMP | 更新时间 |

**activity_registrations**：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 报名记录ID |
| activity_id | BIGINT | 活动ID |
| resident_id | BIGINT | 老人ID |
| registration_status | VARCHAR(50) | 报名状态 |
| created_time | TIMESTAMP | 报名时间 |
| updated_time | TIMESTAMP | 更新时间 |

---

## 📊 业务流程

### 1. 活动发布流程

```
【T1】管理员创建活动
  操作：
  1. 点击"新建活动"
  2. 填写活动信息（标题、类型、日期、位置、人数、描述）
  3. 点击"保存"
  
  系统操作：
  1. 前端表单验证
  2. 后端权限检查
  3. 自动设置status="published"
  4. 记录创建时间

【T2】活动发布成功
  └─ 活动立即对所有老人可见
  └─ 老人可以报名参加

【T3】老人查看和报名
  操作：
  1. 进入"社区活动"页面
  2. 看到"进行中"的活动列表
  3. 点击"查看"了解详情
  4. 点击"报名"进行报名
  
  验证：
  1. 活动必须是"published"状态
  2. 报名人数未满
  3. 未重复报名

【T4】活动举行
  └─ 在activityDate时间点，活动开始举行

【T5】活动完成
  操作：
  1. 管理员点击"完成"
  2. 活动状态变为"completed"
  3. 老人无法再报名
  
【T6】历史查询
  └─ 老人可以看到"已完成"的历史活动
  └─ 用于回顾和评价
```

### 2. 报名流程

```
老人看到活动
  ↓
点击"报名"
  ├─ 检查活动是否还在进行
  ├─ 检查是否已报名
  └─ 检查报名人数是否已满
  ↓
创建报名记录
  └─ registration_status = "registered"
  ↓
显示"报名成功"
  └─ 按钮变为"取消报名"
  ↓
活动日期前的任何时间，可以取消报名
  ↓
取消报名
  └─ 更新registration_status = "cancelled"
```

### 3. 活动人数管理

```
【实时计算当前参与人数】

SELECT COUNT(*)
FROM activity_registrations
WHERE activity_id = ? 
  AND registration_status = 'registered'

【应用场景】
1. 列表显示"已报名"人数
2. 报名时检查是否已满
3. 显示"50/32"（最大人数/当前人数）
4. 关闭报名的条件：currentParticipants >= maxParticipants
```

---

## ⚠️ 已知问题与改进方向

### 问题1：缺少活动提醒机制
**现状**：活动日期定了，但老人可能会忘记  
**改进**：
```java
// 在活动日期前1天发送提醒
LocalDateTime reminderTime = activity.getActivityDate().minusDays(1);
ScheduledTask task = new ScheduledTask()
  .setActivityId(activity.getId())
  .setReminderTime(reminderTime)
  .setType("activity_reminder");

scheduledTaskService.save(task);
```

### 问题2：缺少出席签到功能
**现状**：报名后，无法区分谁实际参加了  
**改进**：
```java
// 活动开始时，管理员进行签到
@PostMapping("/{id}/checkin/{residentId}")
public ApiResponse<String> checkIn(
  @PathVariable Long id,
  @PathVariable Long residentId
) {
  ActivityRegistration registration = ...;
  registration.setRegistrationStatus("attended");  // 改为已参加
  registration.setCheckinTime(LocalDateTime.now());
  registrationService.updateById(registration);
}
```

### 问题3：缺少活动评分和评价
**现状**：活动完成后，老人无法反馈意见  
**改进**：
```
活动完成后，自动弹出评价对话框：
- 评分（1-5星）
- 评价内容（文本）
- 建议

用于改进后续活动质量
```

### 问题4：缺少人数限制的提醒
**现状**：报名人数快满时，没有紧急提醒  
**改进**：
```javascript
// 当已报名人数 >= 最大人数的80%时，标记为"快满"
if (currentParticipants >= maxParticipants * 0.8) {
  // 在列表中标记为红色或添加"快满"标签
  <el-tag type="danger">快满</el-tag>
}
```

### 问题5：缺少活动内容的富文本编辑
**现状**：活动描述只是纯文本，无法插入图片、链接等  
**改进**：
```vue
<!-- 使用富文本编辑器 -->
<quill-editor v-model="formData.activityDescription" />
```

### 问题6：缺少活动分类导航
**现状**：活动类型虽然有筛选，但没有快速导航  
**改进**：
```vue
<!-- 添加分类导航卡片 -->
<div class="activity-categories">
  <el-card class="category-card" @click="selectType('lecture')">
    <div>讲座</div>
    <div class="count">5场</div>
  </el-card>
  <el-card class="category-card" @click="selectType('fitness')">
    <div>健身</div>
    <div class="count">12场</div>
  </el-card>
  ...
</div>
```

### 问题7：缺少老人的已报名活动列表
**现状**：老人无法快速查看自己报名了哪些活动  
**改进**：
```
添加新页面"我的活动"：
- 显示老人已报名的活动
- 按"进行中"、"已参加"、"取消"分类
- 显示活动倒计时
```

---

## 🧪 测试场景

### 1. 创建活动测试

**场景1.1：管理员成功创建活动**
```
角色：admin
操作：
1. 点击"新建活动"
2. 填写所有必填字段
3. 点击"保存"

期望：
- 活动创建成功
- 状态自动为"published"
- 立即对所有用户可见
- 老人可以报名
```

**场景1.2：缺少必填字段**
```
操作：不填活动标题，直接保存

期望：表单验证失败，显示"请输入活动标题"
```

### 2. 报名测试

**场景2.1：老人成功报名**
```
操作：
1. 找到一个"进行中"的活动
2. 点击"报名"
3. 确认报名

期望：
- 报名成功
- 按钮变为"取消报名"
- 活动的"已报名"人数+1
```

**场景2.2：人数已满，无法报名**
```
设置：活动max_participants=50，已报名=50人
操作：尝试报名

期望：提示"报名人数已满"
```

**场景2.3：重复报名**
```
操作：已报名后再次报名

期望：提示"您已经报名过此活动"
```

### 3. 管理员操作测试

**场景3.1：编辑活动**
```
操作：
1. 点击"编辑"
2. 修改活动描述
3. 点击"保存"

期望：活动信息更新成功
```

**场景3.2：标记为完成**
```
操作：点击"完成"

期望：
- 活动状态变为"completed"
- 显示颜色变为蓝色
- 老人无法再报名
- 按钮变为"查看"
```

**场景3.3：取消活动**
```
操作：点击"取消"

期望：
- 活动状态变为"cancelled"
- 颜色变为红色
- 显示"已取消"
```

---

## 📈 实际应用示例

### 示例1：完整的活动流程

```
【12月28日】
管理员李女士计划组织一场养生讲座

【步骤1】创建活动
- 标题：冬季养生知识讲座
- 类型：讲座
- 日期：2026年01月05日 14:00
- 位置：会议室101
- 人数：50人
- 描述：邀请养生专家讲解冬季养生知识...

活动ID：1，状态：published

【步骤2】老人报名
时间          老人名字        操作
2025-12-28    王奶奶          报名成功 (1/50)
2025-12-29    李爷爷          报名成功 (2/50)
2025-12-29    张阿姨          报名成功 (3/50)
...
2026-01-04    王爷爷          报名成功 (45/50)

【步骤3】活动前1天提醒
2026-01-04    系统向45名报名者发送提醒
              提醒：讲座明天14:00举行，请按时到达

【步骤4】活动举行
2026-01-05    14:00 讲座开始
              李女士进行签到：45人参加，5人缺席

【步骤5】标记完成
李女士点击"完成"
活动状态：published → completed

【步骤6】老人反馈
张阿姨给讲座打分：5星
评价："讲座内容很实用，专家讲解得很清楚"

【数据统计】
- 总报名数：50人
- 实际参加：45人
- 参加率：90%
- 平均评分：4.8星
```

### 示例2：活动取消流程

```
【计划】
讲座原定：2026-01-05 14:00
已报名：45人

【突发情况】
讲座专家突然生病，无法到达

【操作】
李女士点击"取消"
活动状态：published → cancelled

【系统操作】
向所有45名报名者发送通知：
"冬季养生讲座因故取消，敬请谅解。
 积分将返还，您可以查看其他活动。"

【数据变化】
- 报名记录保留在历史中
- 老人可以看到"已取消"的活动
- 可用于查询取消原因
```

---

## 📰 资讯模块

除了社区活动外，还有一个资讯模块，用于聚合外部新闻资讯。

### 资讯功能

```
【资讯来源】
外部API：聚合数据头条新闻API

【资讯类型】
- 国内新闻
- 国际新闻
- 养老相关资讯
- 健康资讯

【显示方式】
在"社区活动"页面的资讯栏目中显示
或单独的"资讯"页面

【后端实现】
NewsController 通过HTTP调用外部API
获取新闻数据并转发给前端

【特点】
- 自动更新
- 无需手动维护
- 提供有价值的信息
```

---

## 🔌 与其他模块的集成

### 1. 与通知模块的集成

```
【活动发布时】
→ 发送通知给所有用户
→ 内容：新活动发布

【报名成功时】
→ 发送通知给用户
→ 内容：报名成功

【活动前提醒】
→ 发送提醒通知
→ 内容：活动即将开始
```

### 2. 与评价模块的集成

```
【活动完成后】
→ 自动弹出评价对话框
→ 用户可以对活动评分和评价
→ 用于改进活动质量
```

### 3. 与任务/计划模块的集成

```
【活动报名】
→ 自动创建"参加活动"的任务
→ 活动日期为任务的截止日期
→ 用户可以在任务列表中看到
```

---

## 总结

社区活动与资讯模块是**社区互动和信息传播**的重要工具。

✅ **核心价值**：
- 丰富老人的社区生活
- 组织各类有意义的活动
- 增进老人之间的交流和互动
- 提供有价值的信息资讯

✅ **关键功能**：
- 活动发布和管理（5种类型）
- 报名管理和人数统计
- 活动状态机（draft/published/completed/cancelled）
- 资讯聚合（外部API）

⚠️ **主要改进方向**：
- 活动提醒机制（日期前提醒）
- 出席签到功能（统计实际参加人数）
- 活动评价系统（老人反馈）
- 人数满员提醒（80%时标记快满）
- 富文本编辑器（活动描述）
- 分类快速导航（讲座/健身/娱乐等）
- 个人活动列表（我报名了哪些活动）

该模块体现了**社区服务的人性化和信息化**，让老人能够充分参与社区生活。

